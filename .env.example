# === 必須: Salesforce 認証 ===
SF_USERNAME=user@example.com
SF_PASSWORD=P@ssw0rd123
SF_SECURITY_TOKEN=aBcDeFgHiJkLmNoPqRsTuVwX
SF_BASE_URL=https://yourorg.my.salesforce.com

# === 任意: Salesforce 接続 ===
SF_DOMAIN=login                        # login (production) or test (sandbox)

# === 任意: UI ログイン用 (省略時は SF_USERNAME/PASSWORD を使用) ===
# SF_LOGIN_USERNAME=admin@example.com
# SF_LOGIN_PASSWORD=Adm1nP@ss456

# === 任意: 出力先 (省略時は PROJECT_ROOT/outputs_csv) ===
# CSV_STAGING_DIR=outputs_csv          # 相対パス(cwd基準) or フルパス

# === 任意: notContain フィルタ変換 ===
# SF_NOTCONTAIN_STRICT=false           # true: NOT LIKE 変換を有効化
# SF_NOTCONTAIN_NULL_POLICY=A          # A: NULL OR NOT LIKE / B: NOT LIKE のみ
